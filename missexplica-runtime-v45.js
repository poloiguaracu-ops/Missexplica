/* MissExplica V45 — consolidated runtime guard and stable local features */
(function(){
'use strict';
if(window.__MISSEXPLICA_RUNTIME_V45__)return;window.__MISSEXPLICA_RUNTIME_V45__=true;
const D=document,read=(k,f)=>{try{return JSON.parse(localStorage.getItem(k)||'null')??f}catch{return f}},write=(k,v)=>localStorage.setItem(k,JSON.stringify(v));
const role=()=>{try{return state?.role||'student'}catch{return'student'}},page=()=>{try{return state?.page||'dashboard'}catch{return'dashboard'}},pc=()=>D.getElementById('pageContent');
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
function toast(m,t='info'){let e=D.getElementById('mx45-toast');if(!e){e=D.createElement('div');e.id='mx45-toast';Object.assign(e.style,{position:'fixed',right:'18px',bottom:'18px',zIndex:11000,padding:'12px 15px',borderRadius:'13px',color:'#fff',font:'700 12px/1.35 Inter,system-ui,sans-serif',boxShadow:'0 14px 30px rgba(0,0,0,.18)',maxWidth:'min(420px,calc(100vw - 36px))'});D.body.appendChild(e)}e.textContent=m;e.style.background=t==='error'?'#8d3030':t==='success'?'#0a6b46':'#14251d';clearTimeout(e._t);e._t=setTimeout(()=>e.remove(),2400)}
function clearRuntimeInjected(){const el=pc();if(!el)return;el.querySelectorAll('[data-mx45]').forEach(x=>x.remove())}
function messages(){if(page()!=='messages')return;const el=pc();if(!el||el.dataset.mx45Page==='messages')return;el.dataset.mx45Page='messages';let data=read('missexplica_messages_v1',[]);if(!data.length){data=[{id:1,from:'Equipe MissExplica',subject:'Bem-vindo à MissExplica',body:'Este é o seu espaço de mensagens.',date:new Date().toLocaleDateString('pt-BR'),read:false}];write('missexplica_messages_v1',data)}el.innerHTML=`<div data-mx45="1"><div class="welcome"><div><h1>Mensagens</h1><p>Central de comunicação.</p></div><span class="badge">${data.filter(x=>!x.read).length} não lidas</span></div><div class="grid"><div class="panel"><h3>Caixa de entrada</h3>${data.map(m=>`<article class="notice ${m.read?'':'success'}" style="margin-bottom:10px"><strong>${esc(m.subject)}</strong><small style="float:right">${esc(m.date)}</small><div style="font-size:11px;margin:4px 0">De: ${esc(m.from)}</div><p>${esc(m.body)}</p><button class="action mx45-read" data-id="${m.id}">${m.read?'Lida':'Marcar como lida'}</button> <button class="action mx45-del" data-id="${m.id}">Excluir</button></article>`).join('')}</div><div class="panel"><h3>Nova mensagem</h3><input id="mx45to" placeholder="Destinatário"><input id="mx45sub" placeholder="Assunto"><textarea id="mx45body" rows="6" placeholder="Mensagem"></textarea><button class="green-btn" id="mx45send">Enviar</button></div></div></div>`;el.querySelectorAll('.mx45-read').forEach(b=>b.onclick=()=>{let a=read('missexplica_messages_v1',[]),x=a.find(v=>v.id==b.dataset.id);if(x)x.read=true;write('missexplica_messages_v1',a);rerender()});el.querySelectorAll('.mx45-del').forEach(b=>b.onclick=()=>{write('missexplica_messages_v1',read('missexplica_messages_v1',[]).filter(v=>v.id!=b.dataset.id));rerender()});D.getElementById('mx45send').onclick=()=>{let to=D.getElementById('mx45to').value.trim(),sub=D.getElementById('mx45sub').value.trim(),body=D.getElementById('mx45body').value.trim();if(!to||!sub||!body){toast('Preencha todos os campos.','error');return}let a=read('missexplica_messages_v1',[]);a.unshift({id:Date.now(),from:(state?.user||'Usuário'),to,subject:sub,body,date:new Date().toLocaleDateString('pt-BR'),read:true});write('missexplica_messages_v1',a);toast('Mensagem salva neste dispositivo.','success');rerender()};}
function certificates(){if(role()!=='student'||page()!=='certificates')return;const el=pc();if(!el||el.dataset.mx45Page==='certificates')return;el.dataset.mx45Page='certificates';const p=read('missexplica_course_progress_v1',{}),courses=[['Introdução à Gestão',20],['Atendimento e Vendas',15],['Informática Essencial',25],['Comunicação Profissional',12]],done=courses.filter(c=>p[c[0]]?.completed||c[0]==='Informática Essencial');el.innerHTML=`<div data-mx45="1"><div class="welcome"><h1>Meus certificados</h1><p>Cursos concluídos.</p></div><div class="panel"><h3>Disponíveis</h3>${done.length?done.map(c=>`<div class="course-card" style="margin-bottom:10px"><div class="course-info"><strong>${esc(c[0])}</strong><span>${c[1]} horas • Concluído</span></div><button class="green-btn mx45cert" data-course="${esc(c[0])}">Emitir</button></div>`).join(''):'<div class="empty">Nenhum curso concluído ainda.</div>'}</div></div>`;el.querySelectorAll('.mx45cert').forEach(b=>b.onclick=()=>{let a=read('missexplica_certificates_v1',[]);if(!a.some(x=>x.course===b.dataset.course))a.push({course:b.dataset.course,number:'MX-'+Date.now(),issuedAt:new Date().toISOString()});write('missexplica_certificates_v1',a);toast('Certificado registrado localmente.','success')})}
function settings(){if(!/configurações/i.test(D.getElementById('pageTitle')?.textContent||''))return;const el=pc();if(!el||el.dataset.mx45Page==='settings')return;el.dataset.mx45Page='settings';const p=read('missexplica_profile_v1',{name:localStorage.getItem('missexplica_name')||'Aluno',email:localStorage.getItem('missexplica_email')||'',goal:''});el.innerHTML=`<div data-mx45="1"><div class="welcome"><h1>Configurações</h1><p>Dados deste dispositivo.</p></div><div class="panel"><label>Nome</label><input id="mx45name" value="${esc(p.name)}"><label>E-mail</label><input id="mx45email" value="${esc(p.email)}"><label>Objetivo</label><textarea id="mx45goal" rows="4">${esc(p.goal)}</textarea><button class="green-btn" id="mx45save">Salvar</button></div></div>`;D.getElementById('mx45save').onclick=()=>{const n=D.getElementById('mx45name').value.trim()||'Aluno',e=D.getElementById('mx45email').value.trim(),g=D.getElementById('mx45goal').value.trim();write('missexplica_profile_v1',{name:n,email:e,goal:g});localStorage.setItem('missexplica_name',n);if(e)localStorage.setItem('missexplica_email',e);toast('Perfil salvo neste dispositivo.','success')})}
function rerender(){const el=pc();if(!el)return;el.dataset.mx45Page='';renderSafe()}
let scheduled=false;function renderSafe(){if(scheduled)return;scheduled=true;setTimeout(()=>{scheduled=false;messages();certificates();settings();},0)}
function boot(){if(!pc())return;const mo=new MutationObserver(()=>renderSafe());mo.observe(pc(),{childList:true,subtree:true});renderSafe();window.addEventListener('error',e=>{if(e?.message)console.warn('MissExplica runtime:',e.message)});}
if(D.readyState==='loading')D.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();
