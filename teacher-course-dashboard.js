/* MissExplica — painel do professor com módulos, aulas e ações rápidas. */
(function(){
 const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
 async function loadCourse(courseId){const sb=window.missExplicaSupabase;if(!sb)throw new Error('Supabase não configurado.');const {data,error}=await sb.from('modules').select('id,title,position,lessons(id,title,description,position,published)').eq('course_id',courseId).order('position');if(error)throw error;return data||[]}
 function render(root,course,modules){root.innerHTML=`<div class="teacher-course-dashboard"><div class="course-dash-head"><div><span class="section-tag">SALA DO PROFESSOR</span><h1>${esc(course.title)}</h1><p>Organize módulos, aulas e materiais do seu curso.</p></div><button class="outline-btn" id="backCourses">← Meus cursos</button></div><div class="course-dash-toolbar"><button class="green-btn" id="newModule">+ Novo módulo</button><span>${modules.length} módulo(s)</span></div><div class="teacher-module-list">${modules.map(m=>`<section class="teacher-module"><div class="module-head"><div><small>MÓDULO ${Number(m.position||0)+1}</small><h2>${esc(m.title)}</h2></div><button class="small-btn" data-module="${m.id}">+ Aula</button></div><div class="teacher-lessons">${(m.lessons||[]).map(l=>`<article class="teacher-lesson"><div><span class="lesson-status ${l.published?'published':''}">${l.published?'Publicado':'Rascunho'}</span><strong>${esc(l.title)}</strong><small>${esc(l.description||'Sem descrição')}</small></div><button class="small-btn" data-lesson="${l.id}">Editar</button></article>`).join('')||'<div class="empty-module">Nenhuma aula neste módulo.</div>'}</div></section>`).join('')||'<div class="empty-state"><h3>Comece criando um módulo</h3><p>Depois você poderá adicionar aulas, vídeos e materiais.</p></div>'}</div></div>`}
 window.MissExplicaTeacherCourseDashboard={loadCourse,render};
})();
