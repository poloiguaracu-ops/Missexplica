/* MissExplica — conclusão e certificado visual. */
(function(){
 const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
 function render(root,course,student,progress){const done=progress.percent===100;root.innerHTML=`<section class="certificate-panel ${done?'is-complete':''}"><div class="certificate-icon">${done?'🏆':'🎓'}</div><span class="section-tag">${done?'CURSO CONCLUÍDO':'CERTIFICADO'}</span><h2>${done?'Parabéns, você concluiu!':'Continue seu aprendizado'}</h2><p>${done?`Você concluiu 100% de <strong>${esc(course.title)}</strong>.`:`Conclua todas as aulas de <strong>${esc(course.title)}</strong> para liberar seu certificado.`}</p>${done?`<button class="green-btn" id="certificatePreview">Ver certificado</button>`:`<div class="certificate-progress"><i style="width:${progress.percent}%"></i></div><small>${progress.percent}% concluído</small>`}</section>`;if(done)root.querySelector('#certificatePreview').onclick=()=>openCertificate(course,student)}
 function openCertificate(course,student){const name=esc(student.full_name||student.email||'Aluno');document.body.insertAdjacentHTML('beforeend',`<div class="manager-modal" id="certificateModal"><div class="certificate-sheet"><button class="manager-close">×</button><div class="certificate-border"><span>CERTIFICADO DE CONCLUSÃO</span><h1>MissExplica</h1><p>Certificamos que</p><h2>${name}</h2><p>concluiu com êxito o curso</p><h3>${esc(course.title)}</h3><small>Emitido pela plataforma MissExplica</small></div><button class="green-btn" onclick="window.print()">Imprimir / Salvar PDF</button></div></div>`);document.querySelector('#certificateModal .manager-close').onclick=()=>document.querySelector('#certificateModal')?.remove()}
 window.MissExplicaCertificate={render,openCertificate};
})();
