/* MissExplica — página pública de validação de certificados. */
(function(){
 const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
 function mount(root){root.innerHTML=`<main class="certificate-verifier"><div class="verify-card"><div class="verify-logo">M</div><span class="section-tag">MISSEXPLICA</span><h1>Validar certificado</h1><p>Digite o código do certificado para confirmar sua autenticidade.</p><form id="verifyForm"><label>Código do certificado<input id="certificateCode" autocomplete="off" maxlength="40" placeholder="Ex.: A83F91D20B44" required></label><button class="green-btn" type="submit">Verificar certificado</button></form><div id="verifyOutput"></div></div></main>`;root.querySelector('#verifyForm').onsubmit=async ev=>{ev.preventDefault();const out=root.querySelector('#verifyOutput');out.innerHTML='<p>Verificando...</p>';try{const r=await window.MissExplicaCertificateVerification.verify(root.querySelector('#certificateCode').value);if(!r){out.innerHTML='<div class="verify-invalid">✕ Certificado não encontrado<br><small>Confira o código informado.</small></div>';return}out.innerHTML=`<div class="verify-result"><span class="verify-ok">✓ CERTIFICADO VÁLIDO</span><h2>${esc(r.courses?.title||'Curso')}</h2><p>Emitido para <strong>${esc(r.profiles?.full_name||'Aluno')}</strong>.</p><small>Código: <strong>${esc(r.certificate_code)}</strong><br>Data de emissão: ${new Date(r.issued_at).toLocaleDateString('pt-BR')}</small></div>`}catch(e){out.innerHTML=`<div class="verify-invalid">Não foi possível validar agora.<br><small>${esc(e.message)}</small></div>`}}}
 window.MissExplicaCertificateVerifierPage={mount};
})();
